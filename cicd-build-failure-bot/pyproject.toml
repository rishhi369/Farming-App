[project]
name = "cicd-build-failure-bot"
version = "0.1.0"
description = "Real-time GitHub Actions build failure prediction bot"
requires-python = ">=3.12"

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
